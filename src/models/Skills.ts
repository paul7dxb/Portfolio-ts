export interface PortfolioSkill {
    skillId : string,
    skillName: string,
    skillAbr? : string,
    description: string,
    icon?: string
    featuredSkill?: boolean
}

export interface Skill{
    skillId : string,
    skillName: string,
}

