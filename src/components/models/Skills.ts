export interface PortfolioSkill {
    skillId : string,
    skillName: string,
    skillAbr? : string
    description: string
}

export interface Skill{
    skillId : string,
    skillName: string,
}

